import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3DA9FC',
      contrastText: '#FFFFFF'
    },
    secondary: {
      main: '#3DA9FC',
    },
    primaryBkg: '#000000',
  },
});

export default theme;
